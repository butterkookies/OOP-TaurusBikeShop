Understood. Here’s a structured task list for clarity:

1. Move Filters to Top:

   * Relocate all product filters to a header position above the product cards for both desktop and mobile views.

2. Wishlist Heart Button:

   * Ensure that when a user clicks the heart icon on a product card, it visually fills with color inside the circle to confirm addition.

3. Align Add-to-Cart and Price:

   * Adjust CSS so that the add-to-cart button and price tag are consistently aligned vertically across all product cards.

4. Fix Address Flow:

   * Ensure that the "Add Address" button in checkout works properly and does not redirect or fail; address should be saved and displayed correctly.

5. Resolve SQL Errors:

   * Fix the backend query so all referenced columns (e.g., screenshot URL, storage bucket, storage path, submitted at) exist in the database schema and are spelled correctly.

6. Display Wishlist Items:

   * Ensure that products added to the wishlist appear in the wishlist view page by verifying the data retrieval logic.

7. Remove Redundant Account Prompts:

   * Once a user is logged in, hide or remove any "Create Account" or "Sign In" prompts from the home page or footer.

Use this structured list as a guide to implement the fixes and confirm all parts are tested after each step.
