using System.Windows;

namespace AdminSystem_v2
{
    // Intentionally empty. The shell window lives in Views/MainWindow.xaml.
    public partial class MainWindow : Window
    {
        public MainWindow() => InitializeComponent();
    }
}
